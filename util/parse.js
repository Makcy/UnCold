const request = require('request');
const config = require('../config');
const cheerio = require('cheerio');

module.exports = {
  getUnColdList: async (page = null) => {},
  getUnColdDetail: async () => {}
}